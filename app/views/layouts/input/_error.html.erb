<% if data.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <ul>
        <% data.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>